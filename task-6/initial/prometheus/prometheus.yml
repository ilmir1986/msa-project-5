global:
  scrape_interval: 1s

scrape_configs:
  - job_name: 'pushgateway'
    scrape_interval: 15s
    static_configs:
      - targets: ['pushgateway:9091']
    honor_labels: true

rule_files:
  - "rules/*.yml"